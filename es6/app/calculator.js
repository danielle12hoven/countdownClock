const add = (x, y) => {
  return x + y
}

const minus = (x, y) => {
  return x - y
}

export {add, minus}



// exporting the default method
const multiply = (x, y) => {
  return x * y
}

export default multiply
